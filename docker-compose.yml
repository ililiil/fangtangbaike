version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - VOLC_AK=${VOLC_AK}
      - VOLC_SK=${VOLC_SK}
    restart: unless-stopped
